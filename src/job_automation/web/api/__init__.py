"""
JSON REST API routes. Four files as specified for this milestone, each
covering more than one conceptual resource area — see docs/DASHBOARD.md's
"API endpoints" section for the exact mapping (e.g. match data lives in
`jobs_api.py` since a match is inherently job-scoped; applications live in
`workflow_api.py` since an "application" in this system *is* a workflow at
its later stages).
"""
