# Force LF regardless of the cloning machine's `core.autocrlf` — a shell
# script checked out with CRLF line endings fails inside the Linux
# container with `bad interpreter: /bin/sh^M` (see docker-entrypoint.sh,
# which the Dockerfile's ENTRYPOINT runs directly).
*.sh text eol=lf
Dockerfile text eol=lf
